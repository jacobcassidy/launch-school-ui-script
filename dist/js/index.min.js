// ==UserScript==
// @name         LaunchSchool UI Modifications
// @namespace    https://tampermonkey.net/
// @version      2026-07-22
// @description  Update the Launch School UI for a cleaner, more minimal layout and add additional hotkeys.
// @author       Jacob Cassidy
// @match        https://launchschool.com/**
// @icon         https://www.google.com/s2/favicons?sz=64&domain=launchschool.com
// @grant        none
// ==/UserScript==

(() => {
  // src/js/modules/helpers/state.js
  var elements = {
    injected: {
      header: null,
      settingsMenu: null,
      settingsToggleButton: null,
      sidebarShowButton: null,
      tabsPanelToggleButton: null
    },
    native: {
      contentPanel: null,
      editorPanel: null,
      instructionsPanel: null,
      scratchpad: null,
      sidebar: null,
      tabNav: null,
      tabsPanel: null,
      tocButton: null
    }
  };
  var states = {
    hidden: {
      isHeaderHidden: sessionStorage.getItem("isHeaderHidden") === "true",
      isSidebarHidden: sessionStorage.getItem("isSidebarHidden") === "true" || document.querySelector("#navbar-collapsor").checked,
      isTabsPanelHidden: sessionStorage.getItem("isTabsPanelHidden") === "true"
    },
    hotkeys: {
      cmdShift: {},
      cmdCtrl: {},
      native: {}
    },
    load: {
      isReloadScheduled: false,
      lastUrl: null,
      previousBody: null
    }
  };
  function setElementHeader(el) {
    elements.injected.header = el;
  }
  function setElementSettingsMenu(el) {
    elements.injected.settingsMenu = el;
  }
  function setElementSettingsToggleBtn(el) {
    elements.injected.settingsToggleButton = el;
  }
  function setElementSidebarShowButton(el) {
    elements.injected.sidebarShowButton = el;
  }
  function setElementTabsPanelToggleButton(el) {
    elements.injected.tabsPanelToggleButton = el;
  }
  function setElementContentPanel(el) {
    elements.native.contentPanel = el;
  }
  function setElementEditorPanel(el) {
    elements.native.editorPanel = el;
  }
  function setElementInstructionsPanel(el) {
    elements.native.instructionsPanel = el;
  }
  function setElementScratchpad(el) {
    elements.native.scratchpad = el;
  }
  function setElementSidebar(el) {
    elements.native.sidebar = el;
  }
  function setElementTabsPanel(el) {
    elements.native.tabsPanel = el;
  }
  function setElementTabNav(el) {
    elements.native.tabNav = el;
  }
  function setElementTocButton(el) {
    elements.native.tocButton = el;
  }
  function setAvailableHotkey(modifier, key, symbol, label, callbackFunc = null) {
    let callback;
    if (!callbackFunc) {
      callback = null;
    } else {
      callback = () => callbackFunc();
    }
    states.hotkeys[modifier][key] = { callback, label, symbol };
  }
  function setIsHeaderHidden(value) {
    if (value === true) {
      elements.injected.header.classList.add("is-hidden");
    } else {
      elements.injected.header.classList.remove("is-hidden");
    }
    states.hidden.isHeaderHidden = value;
    sessionStorage.setItem("isHeaderHidden", value);
  }
  function setIsReloadScheduled(value) {
    states.load.isReloadScheduled = value;
  }
  function setIsSidebarHidden(value) {
    const sidebarHideCheckbox = document.querySelector("#navbar-collapsor");
    const sidebarHideBtn = document.querySelector("#navbar-collapse");
    if (!sidebarHideCheckbox) {
      states.hidden.isSidebarHidden = null;
      return;
    }
    const isActiveSidebar = !sidebarHideCheckbox.checked;
    if (value === true) {
      if (isActiveSidebar) {
        sidebarHideBtn.dispatchEvent(
          new MouseEvent("mousedown", {
            bubbles: true,
            cancelable: true,
            button: 0
          })
        );
        sidebarHideBtn.click();
      }
    } else {
      const nativeSidebarShowBtn = document.querySelector("#navbar-expand");
      if (!isActiveSidebar) {
        nativeSidebarShowBtn.dispatchEvent(
          new MouseEvent("mousedown", {
            bubbles: true,
            cancelable: true,
            button: 0
          })
        );
        nativeSidebarShowBtn.click();
      }
    }
    states.hidden.isSidebarHidden = value;
    sessionStorage.setItem("isSidebarHidden", value);
  }
  function setIsTabsPanelHidden(value) {
    const tabsPanel = elements.native.tabsPanel;
    const contentPanel = elements.native.contentPanel;
    const tabsPanelToggleButton = elements.injected.tabsPanelToggleButton;
    if (value === true) {
      tabsPanel.classList.add("hidden", "panel-collapsed");
      contentPanel.classList.remove("half-width");
      tabsPanel.classList.remove("is-active", "half-width");
      tabsPanelToggleButton.classList.remove("active");
    } else {
      tabsPanel.classList.remove("hidden", "panel-collapsed");
      contentPanel.classList.add("half-width");
      tabsPanel.classList.add("is-active", "half-width");
      tabsPanelToggleButton.classList.add("active");
    }
    states.hidden.isTabsPanelHidden = value;
    sessionStorage.setItem("iisTabsPanelHidden", value);
  }
  function setLastUrl(value) {
    states.load.lastUrl = value;
  }
  function setPreviousBody(value) {
    states.load.previousBody = value;
  }

  // src/js/modules/helpers/hide.js
  function hideHeader() {
    setIsHeaderHidden(true);
  }
  function hideSettingsMenu() {
    const settingsMenu = elements.injected.settingsMenu;
    const settingsMenuToggleBtn = elements.injected.settingsToggleButton;
    settingsMenu.classList.remove("active");
    settingsMenuToggleBtn.classList.remove("active");
    document.removeEventListener("pointerdown", handleOutsideSettingsMenuClick);
  }
  function hideSidebar() {
    setIsSidebarHidden(true);
  }
  function hideTabsPanel() {
    setIsTabsPanelHidden(true);
  }
  function hideTocMenu() {
    const tocBtn = elements.native.tocButton;
    if (tocBtn) tocBtn.click();
  }

  // src/js/modules/settings-menu.js
  function injectSettingsMenu(containerEl) {
    const createSettingsMenu = () => {
      const settingsMenuEl = document.createElement("div");
      settingsMenuEl.classList.add("settings-menu", "dropdown-menu");
      const settingsMenuHeaderEl = document.createElement("h2");
      settingsMenuHeaderEl.classList.add("setting-menu__title");
      settingsMenuHeaderEl.innerText = "Settings";
      settingsMenuEl.append(settingsMenuHeaderEl);
      return settingsMenuEl;
    };
    containerEl.append(createSettingsMenu());
  }
  function handleOutsideSettingsMenuClick(e) {
    const settingsMenu = elements.injected.settingsMenu;
    const settingsMenuToggleBtn = elements.injected.settingsToggleButton;
    if (settingsMenu.contains(e.target) || settingsMenuToggleBtn.contains(e.target)) return;
    hideSettingsMenu();
  }

  // src/svg/cmd-key.svg
  var cmd_key_default = '<svg\n  xmlns="http://www.w3.org/2000/svg"\n  viewBox="0 0 24 24"\n  fill="none"\n  stroke="currentColor"\n  stroke-width="2"\n  stroke-linecap="round"\n  stroke-linejoin="round"\n>\n  <g transform="translate(2 2) scale(0.8333)">\n    <path d="M15 6v12a3 3 0 1 0 3-3H6a3 3 0 1 0 3 3V6a3 3 0 1 0-3 3h12a3 3 0 1 0-3-3" />\n  </g>\n</svg>\n';

  // src/svg/tabs-panel.svg
  var tabs_panel_default = '<svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">\n  <g transform="translate(4 4)">\n    <path\n      d="M5.5 0.5H1.5L0.5 1.5V14.5L1.5 15.5H14.5L15.5 14.5V3.5M5.5 0.5V3.5H8H10.5M5.5 0.5H10.5M15.5 3.5H10.5M15.5 3.5V0.5H10.5M10.5 0.5V3.5"\n      stroke="currentColor"\n      stroke-linecap="round"\n      stroke-linejoin="round"\n    />\n  </g>\n</svg>\n';

  // src/js/modules/buttons.js
  function injectSettingsMenuToggleButton(containerEl) {
    const createSettingsMenuToggleButton = () => {
      const settingsMenuToggleButtonEl = document.createElement("button");
      settingsMenuToggleButtonEl.classList.add("site-header__button", "btn--toggle-settings", "has-dropdown");
      settingsMenuToggleButtonEl.title = "Toggle Hotkeys Menu";
      settingsMenuToggleButtonEl.innerHTML = cmd_key_default;
      return settingsMenuToggleButtonEl;
    };
    containerEl.append(createSettingsMenuToggleButton());
  }
  function injectSidebarShowButton(containerEl) {
    const createSidebarShowButton = () => {
      const sidebarShowButtonEl = document.createElement("button");
      sidebarShowButtonEl.classList.add("site-header__button", "btn--show-sidebar");
      sidebarShowButtonEl.title = "Open Menu";
      for (let i = 0; i < 3; i += 1) {
        const lineEl = document.createElement("span");
        lineEl.classList.add("hamburger-line");
        sidebarShowButtonEl.appendChild(lineEl);
      }
      return sidebarShowButtonEl;
    };
    containerEl.appendChild(createSidebarShowButton());
  }
  function injectTabsPanelToggleButton(containerEl) {
    const tabsPanel = elements.native.tabsPanel;
    if (!tabsPanel) return;
    const createTabsPanelToggleButton = () => {
      const tabsPanelToggleButtonEl = document.createElement("button");
      tabsPanelToggleButtonEl.classList.add("site-header__button", "btn--toggle-tabs-panel");
      tabsPanelToggleButtonEl.title = "Toggle Tabs Panel";
      tabsPanelToggleButtonEl.innerHTML = tabs_panel_default;
      return tabsPanelToggleButtonEl;
    };
    containerEl.appendChild(createTabsPanelToggleButton());
  }
  function moveTocBtnToHeader(containerEl) {
    const bookTocBtn = document.querySelector(".toc-toggle-button");
    if (bookTocBtn) {
      bookTocBtn.classList.add("site-header__button", "has-dropdown", ".btn--toggle-toc");
      bookTocBtn.title = "Toggle Table of Contents";
      containerEl.append(bookTocBtn);
    }
  }

  // src/js/modules/header.js
  function injectHeader() {
    const currentDomHeader = document.querySelector(".site-header");
    if (currentDomHeader) {
      return;
    }
    const createHeader = () => {
      const siteHeaderEl = document.createElement("header");
      siteHeaderEl.className = "site-header";
      injectHeaderContainers(siteHeaderEl);
      return siteHeaderEl;
    };
    document.body.insertBefore(createHeader(), document.body.firstChild);
    injectContainerStyleOffsets();
  }
  function injectHeaderContainers(headerEl) {
    for (let i = 0; i < 3; i += 1) {
      const createHeaderContainer = () => {
        const containerEl = document.createElement("div");
        const containerNum = i + 1;
        containerEl.classList.add("site-header__container", `container-${containerNum}`);
        injectContainerElements(containerEl, containerNum);
        return containerEl;
      };
      headerEl.appendChild(createHeaderContainer());
    }
  }
  function injectContainerElements(containerEl, containerNum) {
    if (containerNum === 1) {
      injectSidebarShowButton(containerEl);
      moveLoggedOutNavToHeader(containerEl);
    }
    if (containerNum === 2) {
      const breadcrumbs = document.querySelector(".gretel-breadcrumbs");
      if (breadcrumbs) {
        containerEl.append(breadcrumbs);
      } else {
        injectTitleToHeaderWithNoBreadcrumbs(containerEl);
      }
    }
    if (containerNum === 3) {
      moveTocBtnToHeader(containerEl);
      injectTabsPanelToggleButton(containerEl);
      injectSettingsMenuToggleButton(containerEl);
      injectSettingsMenu(containerEl);
    }
  }
  function injectTitleToHeaderWithNoBreadcrumbs(containerEl) {
    const loggedOutNav = document.querySelector(".columns:has(> #logo + .nav)");
    if (loggedOutNav) return;
    const titleEl = document.querySelector("title");
    const titleText = titleEl.innerText;
    const defaultTitle = "Launch School - an online school for Software Engineers";
    if (titleText === defaultTitle) return;
    const createHeaderTitle = () => {
      const headerTitleEl = document.createElement("div");
      headerTitleEl.classList.add("title-text");
      headerTitleEl.innerHTML = titleText;
      return headerTitleEl;
    };
    containerEl.append(createHeaderTitle());
  }
  function injectContainerStyleOffsets() {
    const container1 = document.querySelector(".site-header__container.container-1");
    const container3 = document.querySelector(".site-header__container.container-3");
    const container1Width = container1?.offsetWidth || 0;
    const container3Width = container3?.offsetWidth || 0;
    const siteHeaderInlinePadding = 12;
    const gapBetweenContainers = 24;
    const sidebarWidth = 195;
    const navExpandBtnWidth = 30;
    const container2LeftOffset = container1Width + siteHeaderInlinePadding + gapBetweenContainers;
    const container2RightOffset = container3Width + siteHeaderInlinePadding + gapBetweenContainers;
    const container2SidebarLeftOffset = sidebarWidth + navExpandBtnWidth + gapBetweenContainers;
    const root = document.documentElement;
    root.style.setProperty("--header-container2-left-offset", `${container2LeftOffset}px`);
    root.style.setProperty("--header-container2-right-offset", `${container2RightOffset}px`);
    root.style.setProperty("--header-container2-sidebar-left-offset", `${container2SidebarLeftOffset}px`);
  }
  function moveLoggedOutNavToHeader(containerEl) {
    const loggedOutNav = document.querySelector(".columns:has(> #logo + .nav)");
    if (loggedOutNav) {
      loggedOutNav.classList.remove("clearfix");
      loggedOutNav.classList.add("logged-out-nav");
      containerEl.appendChild(loggedOutNav);
    }
  }

  // css-text:virtual-esbuild:styles
  var virtual_esbuild_styles_default = ":root{--background-color-001: var(--color-grayscale-098);--background-color-002: var(--color-grayscale-097);--background-color-003: var(--color-grayscale-095);--background-flash-001: var(--color-brand-001-alpha);--background-flash-002: oklch(.75 .1 230 / .15);--border-color-001: var(--color-grayscale-095);--color-brand-001-alpha: #f258581a;--color-brand-001: #f25858;--color-brand-002: #d41010;--color-grayscale-035: oklch(.35 0 0);--color-grayscale-050: oklch(.5 0 0);--color-grayscale-065: oklch(.65 0 0);--color-grayscale-071: oklch(.71 0 0);--color-grayscale-074: oklch(.74 0 0);--color-grayscale-080: oklch(.8 0 0);--color-grayscale-095: oklch(.95 0 0);--color-grayscale-097: oklch(.97 0 0);--color-grayscale-098: oklch(.98 0 0);--color-grayscale-100: oklch(1 0 0);--color-blue-092: oklch(.92 .01 230);--color-blue-095: oklch(.95 .01 230);--header-height: 2.5rem;--nav-drawer-width: 12.1875rem;--duration-long: .5s;--duration-short: .2s;--transition-long: all var(--duration-long) ease;--transition-short: all var(--duration-short) ease;--border-radius-001: 6px;--padding-inline-mw900: calc(50% - 450px) ;--inline-spacing: clamp(1.5rem, -.5rem + 3.125vi, 3rem);@media(width<1996px){--padding-inline-mw900: var(--inline-spacing)}}body,table{background:var(--background-color-001)}a,button{transition:var(--transition-short)}input:where(:not([type=submit])),select,textarea{background-color:var(--color-grayscale-100);border:1px solid var(--border-color-001)!important;border-radius:var(--border-radius-001)!important;box-shadow:none!important;color:var(--color-grayscale-050)!important;min-block-size:40px;padding:12px 16px!important;font-size:.9375rem!important;&.flash-active{animation:bg-flash var(--duration-long)}&:focus{border-color:transparent!important;box-shadow:0 0 0 3px var(--color-blue-092)!important}}@keyframes bg-flash{0%{background-color:var(--background-flash-002)}to{background-color:transparent}}body:not(:has(.nav-drawer)){#header{.show-for-large-up{block-size:0}}#landing-page-header{background-position:initial!important}}.site-header{position:fixed;z-index:200;inset-block-start:0;inset-inline:0;display:flex;align-items:center;justify-content:space-between;gap:var(--inline-spacing);background:var(--background-color-002);border-block-end:1px solid var(--border-color-001);block-size:var(--header-height);padding:4px 12px;transition:var(--transition-long);body:has(.nav-drawer) &{padding-inline-start:var(--nav-drawer-width)}body:not(:has(.nav-drawer)) &{.columns:has(#logo+.nav){float:initial;display:flex;align-items:center;justify-content:space-between;padding:0!important;#logo{margin:0!important;.ir{margin:0!important}}.nav{>ul{display:flex;align-items:center;gap:24px;li{margin:0!important;&.has-dropdown{ul{margin:0!important}&:hover ul,ul:hover{inset-block-start:var(--header-height)!important}}}}}}}&.is-hidden{transform:translateY(-100%)}.site-header__container{display:flex;align-items:center;gap:6px;max-inline-size:100%;&.container-1,&.container-3{min-inline-size:fit-content}&.container-1{justify-content:flex-start;&:has(.logged-out-nav){flex-grow:1}.logged-out-nav{gap:var(--inline-spacing)}}&.container-2{position:absolute;inset-inline-start:var(--header-container2-left-offset);justify-content:center;inline-size:calc(100% - var(--header-container2-left-offset) - var(--header-container2-right-offset));transition:var(--transition-long);body:has(#navbar-collapsor):not(:has(#navbar-collapsor:checked)) &{inset-inline-start:var(--header-container2-sidebar-left-offset);inline-size:calc(100% - var(--header-container2-sidebar-left-offset) - var(--header-container2-right-offset))}}&.container-3{justify-content:flex-end}}.gretel-breadcrumbs,.title-text{display:flex!important;align-items:center!important;overflow:hidden;text-overflow:ellipsis;color:var(--color-grayscale-074);block-size:var(--header-height)!important;margin:0!important;padding:2px 0 0!important;font-family:AvenirNext,Avenir,myriad-pro,myriad pro,Helvetica Neue,Helvetica,Roboto,Arial,sans-serif;font-size:.875rem;font-weight:700;white-space:nowrap}.gretel-breadcrumbs{position:static!important;border-block-end:1px solid var(--border-color-001)!important;:is(li,li a){color:var(--color-grayscale-071)!important;white-space:nowrap}li{a{&:hover{color:var(--color-brand-001)!important}}&:last-child{padding-inline-end:0}}li.current{overflow:hidden;text-overflow:ellipsis;color:var(--color-grayscale-074)!important}}body:has(#navbar-collapsor:checked) &,body:not(:has(.nav-drawer)) &{padding-inline-start:12px!important}body:not(:has(#navbar-collapsor:checked)) &{.btn--show-sidebar{display:none}}}#navbar-collapsor:checked~.nav-drawer{inline-size:0!important}#navbar-expand{display:none}.resize-handle{pointer-events:none;display:block;background-color:var(--border-color-001)!important;inline-size:4px!important;body:has(.tabs-panel.panel-collapsed) &{display:none}}:is(.assignment-content-panel,.book-content-panel,.editor-panel,.instructions-panel,.tabs-panel){background-color:var(--background-color-001)!important;padding:0!important}:is(.assignment-content-panel,.book-content-panel,.tabs-panel){&.half-width{flex-basis:calc(50% - 2px)!important}}:is(.editor-panel,.instructions-panel){flex-basis:calc(50% - 2px)!important}:is(.assignment-content-panel,.book-content-panel){flex-basis:100%!important;background:var(--background-color-001);border:none!important;padding:0!important;:is(.assignment-content-wrapper,.book-content-wrapper){max-inline-size:100%!important;margin:0!important;padding:40px var(--inline-spacing)!important;>*{inline-size:100%!important;max-inline-size:780px!important;margin:0 auto!important}}}:is(.assignment-repl-tabs,.book-exercise-repl-tabs,.book-repl-tabs,.repl-tabs){border:none!important;max-inline-size:initial!important}.tabs-panel{display:none!important;border:none!important;&.is-active{display:flex!important}&.panel-collapsed{flex-basis:0!important}body:not(:has(.nav-drawer)) &{padding:0!important}}.site-header__button{cursor:pointer;position:static!important;display:flex;flex-shrink:0;align-items:center;justify-content:center;background-color:var(--background-color-003)!important;border-radius:var(--border-radius-001)!important;color:var(--color-grayscale-050);block-size:32px!important;inline-size:32px!important;margin:0;padding:6px;svg{block-size:20px}&:hover{background-color:var(--color-brand-001)!important;color:var(--color-grayscale-100)}&.active{background-color:var(--color-brand-001-alpha)!important;color:var(--color-brand-002)}&.btn--show-sidebar{flex-direction:column;gap:3px;.hamburger-line{background-color:var(--color-grayscale-050);block-size:2px;inline-size:16px;transition:var(--transition-short)}&:hover{.hamburger-line{background-color:var(--color-grayscale-100)}}}&.toc-toggle-button{&.open{background-color:var(--color-brand-001-alpha)!important;color:var(--color-brand-002)!important}}}.settings-menu{position:absolute;z-index:101;inset-block-start:calc(var(--header-height) + 8px);inset-inline-end:12px;display:none;flex-direction:column;gap:12px;overflow-block:auto;overflow-inline:hidden;background:#fff;border:1px solid var(--border-color-001);border-radius:var(--border-radius-001);box-shadow:0 4px 12px #00000026;scrollbar-color:#d1d5db #f8f9fa;scrollbar-width:thin;max-block-size:calc(100vb - var(--header-height) - 8px - 16px);inline-size:420px;padding:16px;transition:var(--transition-long);:where(h1,h2,h3,h4,ul,li){color:var(--color-grayscale-050);margin:0}:where(ul){list-style-type:none;margin:0}:where(section){display:flex;flex-direction:column;gap:8px}.setting-menu__title{display:none;font-size:1.25rem}.section-title{font-size:1rem;font-weight:600}.settings-list-wrapper{display:grid;grid-template-columns:max-content 1fr;row-gap:12px}.settings-list{display:grid;grid-column:1 / -1;grid-template-columns:subgrid;border:1px solid var(--border-color-001);border-radius:var(--border-radius-001);&:has(.settings-list__title:only-child){display:none}}.settings-list__title{grid-column:1 / -1;border-block-end:1px solid var(--border-color-001);color:var(--color-grayscale-080);padding:8px 12px;font-size:.75rem;font-weight:700;line-height:1.5;text-transform:uppercase}.settings-list__item{display:grid;grid-column:1 / -1;grid-template-columns:subgrid;border-block-end:1px solid var(--border-color-001);.setting-status,.setting-desc{display:flex;align-items:center;padding:8px 12px;line-height:1.5}.setting-status{grid-column:1;gap:2px;border-inline-end:1px solid var(--border-color-001);color:var(--color-grayscale-065);font-family:Monaco,Menlo,Consolas,monospace;font-size:.75rem;.key{background-color:var(--color-grayscale-097);border-radius:4px;padding:1px 4px;&:last-child{background-color:var(--color-brand-001-alpha);color:var(--color-brand-001);padding-inline:6px;font-weight:700}}}.setting-desc{grid-column:2;color:var(--color-grayscale-050);font-size:.8125rem}&:nth-child(2n){background-color:var(--color-grayscale-098)}&:last-child{border-block-end-width:0}}&.active{display:flex}}.tab-nav{align-items:center;gap:0!important;background-color:var(--background-color-001)!important;border-color:var(--border-color-001)!important;border-block-end:1px solid var(--border-color-001)!important;block-size:var(--header-height);margin:0!important;padding:0 var(--padding-inline-mw900)!important;@media(width<1900px){padding-inline-start:0!important}}.tab-button{align-items:center!important;gap:12px;transform:none!important;border:1px solid transparent!important;border-radius:0!important;border-inline-end-color:var(--border-color-001)!important;color:var(--color-grayscale-074)!important;block-size:calc(100% + 1px)!important;margin:0 0 -1px!important;padding:2px var(--inline-spacing) 0!important;font-weight:700!important;&:first-child{border-inline-start:1px solid var(--border-color-001)!important;body:has(#assignment-repl-layout,#book-repl-layout,#book-exercise-repl-layout,#exercise-repl-layout) &{margin:0!important}@media(width<1900px){border-inline-start:none!important}}i{padding-block-end:2px;font-weight:700!important}svg{path{transition:var(--transition-short)}}&:where(:not(.active)){svg{path{fill:var(--color-grayscale-074)!important}}&:hover{color:var(--color-brand-001)!important;svg{path{fill:var(--color-brand-001)!important}}}}&.active{border-block-end-color:var(--background-color-001)!important;color:var(--color-grayscale-050)!important}&:active{color:var(--color-brand-002)!important;svg{path{fill:var(--color-brand-002)!important}}}&.flash-active{animation:bg-flash var(--duration-long)}}.tab-content{margin:0!important;padding:0!important;.tab-header{align-items:center;background-color:var(--background-color-001)!important;border-block-end:1px solid var(--border-color-001)!important;block-size:var(--header-height);margin:0!important;padding-inline:var(--padding-inline-mw900)!important;.tab-header-actions{gap:8px!important;.lsbot-tab-button{padding:0!important}}body:has(#assignment-repl-layout) &{padding-inline-end:var(--padding-inline-mw900)!important}}body:has(#book-repl-layout) &{margin-inline-end:0!important}.tab-pane:not(:has(.tab-header),:has(.editor-mode-bar)){padding:24px var(--padding-inline-mw900)!important}#tab-feedback{body:has(#assignment-repl-layout,#book-repl-layout,#book-exercise-repl-layout,#exercise-repl-layout) &{.feedback-form-inline form{textarea.form-control{border:1px solid var(--border-color-001)!important;padding:12px 16px!important;font-size:.9375rem!important;&:focus{border-color:transparent!important;box-shadow:0 0 0 3px var(--color-blue-092)!important}}}}}#tab-instructions{#actions.gray-links{border-color:var(--border-color-001)!important;button,a{border:1px solid var(--border-color-001)}}}.community-solutions-section{background-color:var(--background-color-001)!important;border:none!important;box-shadow:none!important;margin:0!important;padding:0!important}}.toast-container{pointer-events:none;position:fixed;z-index:99999;inset-block-start:12px;inset-inline-end:12px;display:flex;flex-direction:column;gap:12px}.toast{transform:translateY(-10px);background:var(--color-grayscale-035);border-radius:var(--border-radius-001);color:#fff;opacity:0;padding:16px 20px;transition:opacity var(--duration-long) ease,transform var(--duration-long) ease}.toast.show{transform:translateY(0);opacity:.9}.toc-dropdown-menu{border-color:var(--border-color-001)!important;max-block-size:calc(100vb - var(--header-height) - 8px - 16px)!important;transition:var(--transition-long);body:has(.site-header.is-hidden) &{inset-block-start:calc(100% - var(--header-height) + 8px)!important;max-block-size:calc(100vb - 24px)!important}}.toc-dropdown-content{padding:16px!important;.chapters{display:flex;flex-direction:column;gap:32px;li{padding:0!important}ul{display:flex;flex-direction:column;gap:8px;margin:0!important}>li{display:flex;flex-direction:column;gap:12px}.toc-navigate-links{display:flex;flex-direction:column;gap:8px;border-color:var(--border-color-001)!important;margin:0!important;padding-block-end:8px!important;span{margin:0!important;&:first-child{font-weight:600}}}.toc-section{margin:0!important;.section-title{padding:0!important}.topics{display:flex;flex-direction:column;gap:8px;margin:0!important;padding-inline-start:12px!important;li.chapter_title{display:flex;flex-direction:column;gap:12px;padding:0!important}}.sub_topics{margin:0!important;padding-inline-start:36px!important}}}}:is(.editor-panel,#tab-code-editor){.editor-mode-bar{background-color:var(--background-color-001)!important;border-block-end:1px solid var(--border-color-001)!important;block-size:var(--header-height)!important;inline-size:100%!important;max-inline-size:100%!important;margin:0!important;padding-inline:var(--padding-inline-mw900)!important}.tab-pane .CodeMirror{margin-inline-end:16px}:is(.code-editor-section,.editor-content-wrapper){max-inline-size:100%!important;padding-inline:var(--padding-inline-mw900)!important;.code-editor-wrapper,.code-editor-wrapper .CodeMirror,.code-editor-wrapper .CodeMirror .CodeMirror-scroll,.CodeMirror-gutters,#exercise-code-editor{background-color:var(--background-color-002)!important}.code-editor-wrapper{border:1px solid var(--border-color-001);border-radius:var(--border-radius-001);margin-block-start:24px;padding:12px!important;#exercise-code-editor{border:none!important}.CodeMirror-activeline-background{background-color:var(--background-color-003)!important;.CodeMirror-focused &{background-color:var(--color-blue-095)!important}}.CodeMirror-activeline{.flash-active &{.CodeMirror-line{animation:bg-flash var(--duration-long)}}}.CodeMirror-lines,.CodeMirror-linenumber{padding:0!important}.CodeMirror-gutters{border-color:var(--border-color-001);padding:0}}#btn-run-code{margin:16px 0 24px auto!important}}.output-section-fixed{align-self:center;border:none!important;inline-size:calc(100% - (var(--inline-spacing) * 2));max-inline-size:900px;margin:0 0 24px!important;#code-output-content{background-color:var(--background-color-001)!important}}}:is(.conversation-area,.lsbot-input-area){padding-inline:var(--padding-inline-mw900)!important}.conversation-area{body:has(#assignment-repl-layout,#book-repl-layout,#book-exercise-repl-layout,#exercise-repl-layout) &{:is(.assignment-repl-layout,.book-repl-layout,.book-exercise-repl-layout,.exercise-repl-layout) &{background:var(--background-color-001)!important;padding-block:24px!important}}.message-content{.lsbot-message &{background:var(--color-blue-095)!important;border:none!important;padding:12px 16px!important}.lsbot-feedback-bar{background:transparent!important;border:none!important;padding:0!important}.lsbot-feedback-context,.lsbot-feedback-btn,.timestamp{color:var(--color-grayscale-074)!important}}.detected-solution-box{.detected-solution-code{pre{padding:16px!important;code{margin-inline-start:32px!important;padding:0!important;.line-numbers-rows{inset-inline-start:-48px!important;border-color:var(--border-color-001)!important;>span:before{color:var(--color-grayscale-074)!important}}}}}}}.lsbot-input-area,.lsbot-question-input{border-color:var(--border-color-001)!important}.lsbot-input-area{margin:0!important;padding-block:16px!important}.lsbot-input-wrapper{.book-exercise-completion-status,.exercise-completion-status{position:static!important;color:var(--color-grayscale-074)!important;margin:-8px 0 8px!important;padding:0 var(--padding-inline-mw900)!important;i{color:var(--color-grayscale-074)!important}}}.books.show-page .large-11{display:flex;align-items:center}.navigate-back,.toc-button-text,.toc-chevron{display:none}.books.read-page{#book-repl-layout{.toc-dropdown-container{inset-block-start:var(--header-height)!important;inset-inline:auto 360px!important}}}.nav-tabs.secondary-nav{margin:calc(var(--header-height) * -1) 0 var(--header-height) 0;padding-block-start:var(--header-height)}.wrapper{margin:0!important;padding:calc(var(--header-height) * 2) 0 0 var(--nav-drawer-width)!important;body:has(#navbar-collapsor:checked) &,body:not(:has(.nav-drawer)) &{padding-inline-start:0!important}}body:not(:has(.assignment-repl-layout,.book-repl-layout)){.wrapper{body:has(.site-header.is-hidden) &{inset-block-start:calc(var(--header-height) * -1)!important}}}body:has(#assignment-repl-layout,#book-repl-layout,#book-exercise-repl-layout,#exercise-repl-layout){:is(.assignment-repl-layout,.book-repl-layout,.book-exercise-repl-layout,.exercise-repl-layout){inset-block-start:var(--header-height)!important;inset-inline-start:0!important;padding:0 0 0 var(--nav-drawer-width)!important;transition:var(--transition-long);body:has(.nav-drawer) &{inset-block-start:var(--header-height)!important}body:has(.site-header.is-hidden) &{inset-block-start:0!important}body:has(#navbar-collapsor:checked) &,body:not(:has(.nav-drawer)) &{inset-inline-start:0!important;padding:0!important}}}\n";

  // src/js/modules/helpers/style.js
  function injectStyles() {
    const existingStyles = document.getElementById("ls-ui-script-styles");
    if (existingStyles) return;
    const createStyleEl = () => {
      const style = document.createElement("style");
      style.setAttribute("id", "ls-ui-script-styles");
      style.textContent = virtual_esbuild_styles_default;
      return style;
    };
    document.head.appendChild(createStyleEl());
  }

  // src/js/modules/toaster.js
  function injectToaster() {
    const createToaster = () => {
      const toasterEl = document.createElement("div");
      toasterEl.classList.add("toast-container");
      return toasterEl;
    };
    document.body.appendChild(createToaster());
  }

  // src/js/modules/helpers/activate.js
  function activateHotkey(modifier, eventCode) {
    const keyEvents = states.hotkeys[modifier];
    for (const [key, keyObj] of Object.entries(keyEvents)) {
      if (key === eventCode) {
        keyObj.callback();
      }
    }
  }
  function activateTab(tabBtn) {
    tabBtn.dispatchEvent(
      new MouseEvent("mousedown", {
        bubbles: true,
        cancelable: true,
        button: 0
      })
    );
    tabBtn.click();
  }

  // src/js/modules/helpers/show.js
  function showHeader() {
    setIsHeaderHidden(false);
  }
  function showSettingsMenu() {
    const settingsMenu = elements.injected.settingsMenu;
    const settingsMenuToggleBtn = elements.injected.settingsToggleButton;
    settingsMenu.classList.add("active");
    settingsMenuToggleBtn.classList.add("active");
    const openedTocMenu = document.querySelector(".toc-toggle-button.open");
    if (openedTocMenu) openedTocMenu.click();
    document.addEventListener("pointerdown", handleOutsideSettingsMenuClick);
  }
  function showSidebar() {
    setIsSidebarHidden(false);
  }
  function showTabsPanel() {
    setIsTabsPanelHidden(false);
  }
  function showToast(message, duration = 2500) {
    const toastContainer = document.querySelector(".toast-container");
    const toast = document.createElement("div");
    toast.className = "toast";
    toast.textContent = message;
    toastContainer.appendChild(toast);
    requestAnimationFrame(() => {
      toast.classList.add("show");
    });
    setTimeout(() => {
      toast.classList.remove("show");
      toast.addEventListener("transitionend", () => toast.remove(), { once: true });
    }, duration);
  }
  function showTocMenu() {
    const tocBtn = elements.native.tocButton;
    if (tocBtn) {
      tocBtn.click();
      const settingsMenu = elements.injected.settingsMenu;
      if (settingsMenu && settingsMenu.classList.contains("active")) hideSettingsMenu();
    }
  }

  // src/js/modules/helpers/toggle.js
  function toggleHeader() {
    const isHeaderHidden = states.hidden.isHeaderHidden;
    if (isHeaderHidden) {
      showHeader();
    } else {
      hideHeader();
    }
  }
  function toggleExerciseStatus() {
    const statusToggleButton = document.querySelector(".edit_exercise_submission .button");
    if (statusToggleButton.disabled) {
      showToast("Warning: exercise status change is still in progress.");
      return;
    }
    const markIncomplete = document.querySelector(".edit_exercise_submission input[value='delete']");
    console.log(markIncomplete);
    let toastMsg;
    if (markIncomplete) toastMsg = "Exercise marked incomplete.";
    else toastMsg = "Exercise marked complete.";
    statusToggleButton.dispatchEvent(
      new MouseEvent("mousedown", {
        bubbles: true,
        cancelable: true,
        button: 0
      })
    );
    statusToggleButton.click();
    showToast(toastMsg);
  }
  function toggleSettingsMenu() {
    if (elements.injected.settingsMenu.classList.contains("active")) {
      hideSettingsMenu();
    } else {
      showSettingsMenu();
    }
  }
  function toggleSidebar() {
    const isSidebarHidden = states.hidden.isSidebarHidden;
    if (isSidebarHidden) {
      showSidebar();
    } else {
      hideSidebar();
    }
  }
  function toggleTabsPanel() {
    const isTabsPanelHidden = states.hidden.isTabsPanelHidden;
    if (isTabsPanelHidden) {
      showTabsPanel();
    } else {
      hideTabsPanel();
    }
  }
  function toggleTocMenu() {
    const tocBtn = elements.native.tocButton;
    if (tocBtn && tocBtn.classList.contains("open")) {
      hideTocMenu();
    } else if (tocBtn) {
      showTocMenu();
    }
  }

  // src/js/modules/helpers/flash.js
  function flashActiveElement(activeEl, secondaryActiveEl) {
    const isTabButton = activeEl.classList.contains("tab-button");
    if (isTabButton) {
      flashWithClass(activeEl, secondaryActiveEl);
    } else {
      flashWithClass(activeEl);
    }
  }
  function flashWithClass(flashEl, secondaryFlashEl) {
    const elementsToFlash = [flashEl];
    const flashDuration = 300;
    if (secondaryFlashEl) elementsToFlash.push(secondaryFlashEl);
    elementsToFlash.forEach((el) => {
      clearTimeout(el.flashTimeout);
      cancelAnimationFrame(el.flashFrame);
      el.classList.remove("flash-active");
      el.flashFrame = requestAnimationFrame(() => {
        el.classList.add("flash-active");
        el.flashTimeout = setTimeout(() => {
          el.classList.remove("flash-active");
          el.flashTimeout = null;
        }, flashDuration);
      });
    });
  }

  // src/js/modules/helpers/focus.js
  function handleFocus(focusEl) {
    const isTextarea = focusEl instanceof HTMLTextAreaElement;
    const isTabButton = focusEl.classList.contains("tab-button");
    const getTabTextareaElement = () => {
      const tabButtonData = focusEl?.dataset?.tab;
      if (!tabButtonData) return;
      const tabContainerId = `tab-${tabButtonData}`;
      const tabContainer = document.getElementById(tabContainerId);
      if (!tabContainer) return;
      const tabContainerTextarea = tabContainer.querySelector(".CodeMirror textarea") || tabContainer.querySelector("textarea") || null;
      return tabContainerTextarea;
    };
    if (isTabButton) {
      const activeTab = document.querySelector(".tab-button.active, .tab-button[aria-selected='true']");
      const isActiveTab = activeTab?.dataset?.tab === focusEl.dataset?.tab;
      const tabTextarea = getTabTextareaElement();
      const isTextareaFocused = document.activeElement === tabTextarea;
      const isTabsPanelHidden = states.hidden.isTabsPanelHidden;
      const codeMirror = tabTextarea?.closest(".CodeMirror");
      const isCodeMirrorFocused = codeMirror?.classList.contains("CodeMirror-focused");
      const focusTabTextarea = () => {
        setTimeout(() => {
          tabTextarea.focus();
        }, 100);
      };
      if (isTabsPanelHidden) showTabsPanel();
      if (isActiveTab) {
        if (isCodeMirrorFocused) {
          flashActiveElement(focusEl, codeMirror);
          return;
        }
        if (isTextareaFocused) {
          flashActiveElement(focusEl, tabTextarea);
          return;
        }
        if (tabTextarea && !isTextareaFocused) {
          focusTabTextarea();
          flashActiveElement(tabTextarea);
          return;
        }
        if (!tabTextarea) {
          flashActiveElement(focusEl);
          return;
        }
      } else {
        if (tabTextarea && !isTextareaFocused) {
          focusTabTextarea();
          return;
        }
      }
    }
    if (isTextarea) {
      const codeMirror = focusEl.closest(".CodeMirror");
      const isCodeMirrorFocused = codeMirror?.classList.contains("CodeMirror-focused");
      if (isCodeMirrorFocused) {
        flashActiveElement(codeMirror);
      } else {
        focusEl.focus();
      }
      return;
    }
  }

  // src/js/modules/helpers/sync.js
  function syncAvailableHotkeys() {
    states.hotkeys.cmdShift = {};
    states.hotkeys.cmdCtrl = {};
    states.hotkeys.native = {};
    const headerExists = elements.injected.header;
    const settingsMenuExists = elements.injected.settingsMenu;
    const copyCodeBtnExists = document.querySelector(".btn-copy-code");
    const editorExists = elements.native.editorPanel;
    const instructionsPanelExists = elements.native.instructionsPanel;
    const markExerciseBtnExists = document.querySelector(".edit_exercise_submission .button");
    const scratchpadExists = elements.native.scratchpad;
    const sidebarExists = elements.native.sidebar;
    const submitReviewBtnExists = document.querySelector("#lsbot-send-review");
    const tabNavExists = elements.native.tabNav;
    const tabsPanelExists = elements.native.tabsPanel;
    const tocButtonExists = elements.native.tocButton;
    let nextExerciseLinkExists, nextExerciseLink;
    if (instructionsPanelExists) {
      nextExerciseLink = [...document.querySelectorAll("a")].find(
        (a) => a.textContent.includes("Go to the next exercise")
      );
      if (nextExerciseLink) nextExerciseLinkExists = true;
    }
    const handleEditorHotkey = (modifier) => {
      const editorPanel = elements.native.editorPanel;
      let focusEl;
      let label;
      const focusEditorPanel = () => {
        const codeEditor = editorPanel.querySelector(".CodeMirror textarea");
        if (codeEditor) handleFocus(codeEditor);
      };
      const focusScratchpad = () => {
        const scratchpadTab = document.querySelector(".tab-button[data-tab='code-editor']");
        if (scratchpadTab) activateTab(scratchpadTab);
      };
      if (editorExists) {
        focusEl = focusEditorPanel;
        label = "Focus Editor";
      } else {
        focusEl = focusScratchpad;
        label = "Focus Scratchpad Editor";
      }
      setAvailableHotkey(modifier, "KeyE", "E", label, focusEl);
    };
    const syncCmdShiftHotkeys = () => {
      if (headerExists) setAvailableHotkey("cmdShift", "Digit1", 1, "Toggle Header", toggleHeader);
      if (tabsPanelExists) setAvailableHotkey("cmdShift", "Digit2", 2, "Toggle Tabs Panel", toggleTabsPanel);
      if (editorExists || scratchpadExists) handleEditorHotkey("cmdShift");
    };
    const syncCmdCtrlHotkeys = () => {
      const handleCopyCodeHotkey = () => {
        const triggerCopyBtn = () => {
          const copyBtn = document.querySelector(".btn-copy-code");
          copyBtn.dispatchEvent(
            new MouseEvent("mousedown", {
              bubbles: true,
              cancelable: true,
              button: 0
            })
          );
          copyBtn.click();
          if (elements.native.scratchpad) showToast("Scratchpad code copied");
          else if (elements.native.editorPanel) showToast("Editor code copied");
          else showToast("Code copied");
        };
        let label;
        if (elements.native.scratchpad) label = "Copy Scratchpad Code";
        else label = "Copy Editor Code";
        setAvailableHotkey("cmdCtrl", "KeyC", "C", label, triggerCopyBtn);
      };
      const handleNextExerciseHotkey = () => {
        showToast("Going to next exercise");
        nextExerciseLink.click();
      };
      const handleSubmitReviewHotkey = () => {
        const reviewSubmitBtn = document.querySelector("#lsbot-send-review");
        const reviewTabBtn = document.querySelector(".tab-button[data-tab='submit-review']");
        activateTab(reviewTabBtn);
        setTimeout(() => {
          reviewSubmitBtn.click();
        }, 100);
        showToast("Solution submitted for LSBot Review");
      };
      const handleTabsHotkeys = () => {
        const allTabBtns = document.querySelectorAll(".tab-button");
        const tabs = [];
        allTabBtns.forEach((btn) => {
          const isHidden = getComputedStyle(btn).display === "none";
          if (isHidden) return;
          const label = btn.getAttribute("aria-label") || btn.textContent.trim();
          const fullLabel = `Focus ${label} Tab`;
          tabs.push([btn, fullLabel]);
        });
        tabs.forEach((tab, index) => {
          const btnEl = tab[0];
          const btnLabel = tab[1];
          const key = index + 1;
          const eventCode = `Digit${key}`;
          const triggerTab = () => activateTab(btnEl);
          setAvailableHotkey("cmdCtrl", eventCode, key, btnLabel, triggerTab);
        });
      };
      if (tabNavExists) handleTabsHotkeys();
      if (sidebarExists) setAvailableHotkey("cmdCtrl", "KeyB", "B", "Toggle Sidebar Menu", toggleSidebar);
      if (copyCodeBtnExists) handleCopyCodeHotkey();
      if (editorExists || scratchpadExists) handleEditorHotkey("cmdCtrl");
      if (markExerciseBtnExists)
        setAvailableHotkey("cmdCtrl", "KeyM", "M", "Toggle Exercise Status", toggleExerciseStatus);
      if (nextExerciseLinkExists)
        setAvailableHotkey("cmdCtrl", "KeyN", "N", "Go to next exercise", handleNextExerciseHotkey);
      if (submitReviewBtnExists) setAvailableHotkey("cmdCtrl", "KeyR", "R", "Submit Review", handleSubmitReviewHotkey);
      if (tocButtonExists) setAvailableHotkey("cmdCtrl", "KeyT", "T", "Toggle Table of Content", toggleTocMenu);
      if (settingsMenuExists) setAvailableHotkey("cmdCtrl", "Comma", ",", "Toggle Hotkeys Menu", toggleSettingsMenu);
    };
    syncCmdShiftHotkeys();
    syncCmdCtrlHotkeys();
  }
  function syncInjectedElementsState() {
    const header = document.querySelector(".site-header");
    const settingsMenu = document.querySelector(".settings-menu");
    const settingsToggleBtn = document.querySelector(".btn--toggle-settings");
    const sidebarShowBtn = document.querySelector(".btn--show-sidebar");
    const tabsPanelToggleBtn = document.querySelector(".btn--toggle-tabs-panel");
    setElementHeader(header);
    setElementSettingsMenu(settingsMenu);
    setElementSettingsToggleBtn(settingsToggleBtn);
    setElementSidebarShowButton(sidebarShowBtn);
    setElementTabsPanelToggleButton(tabsPanelToggleBtn);
  }
  function syncNativeElementsState() {
    const contentPanel = document.querySelector(".assignment-content-panel") || document.querySelector(".book-content-panel");
    const editorPanel = document.querySelector(".editor-panel");
    const scratchpad = document.querySelector("#tab-code-editor");
    const instructionsPanel = document.querySelector(".instructions-panel");
    const sidebar = document.querySelector(".nav-drawer");
    const tabNav = document.querySelector(".tab-nav");
    const tabsPanel = document.querySelector(".tabs-panel");
    const tocButton = document.querySelector(".toc-toggle-button");
    setElementContentPanel(contentPanel);
    setElementEditorPanel(editorPanel);
    setElementInstructionsPanel(instructionsPanel);
    setElementScratchpad(scratchpad);
    setElementSidebar(sidebar);
    setElementTabNav(tabNav);
    setElementTabsPanel(tabsPanel);
    setElementTocButton(tocButton);
  }

  // src/js/modules/helpers/log.js
  var colorLog = {
    alert: (msg, ...args) => console.log(`%c${msg}`, "color: #f00; font-weight: 700;", ...args),
    debug: (msg, ...args) => console.log(`%c${msg}`, "color: #f80;", ...args),
    detail: (msg, ...args) => console.log(`%c${msg}`, "color: #999;", ...args),
    info: (msg, ...args) => console.log(`%c${msg}`, "color: #007acc;", ...args),
    notice: (msg, ...args) => console.log(`%c${msg}`, "color: #f08; font-weight: 700;", ...args),
    run: (msg, ...args) => console.log(`%c${msg}`, "color: #28a745;", ...args)
  };

  // src/js/modules/helpers/watch.js
  function watchForMissingHeader() {
    if (document.documentElement.dataset.headerObserverBound) {
      return;
    }
    document.documentElement.dataset.headerObserverBound = "true";
    let headerMissingTimeoutId;
    const observer = new MutationObserver(() => {
      if (document.querySelector(".site-header")) {
        clearTimeout(headerMissingTimeoutId);
        headerMissingTimeoutId = void 0;
        return;
      }
      if (headerMissingTimeoutId) return;
      headerMissingTimeoutId = setTimeout(() => {
        headerMissingTimeoutId = void 0;
        const siteHeader = document.querySelector(".site-header");
        if (siteHeader) {
        } else {
          colorLog.detail(".site-header has been missing for over 300ms. Scheduling reload.");
          scheduleReload();
        }
      }, 300);
    });
    observer.observe(document.documentElement, {
      childList: true,
      subtree: true
    });
  }
  function watchForUrlChange() {
    if (document.documentElement.dataset.watchPageBound) {
      return;
    }
    document.documentElement.dataset.watchPageBound = "true";
    const originalPushState = history.pushState;
    const originalReplaceState = history.replaceState;
    const checkForUrlChange = (from) => {
      colorLog.detail(`checkForUrlChange() called from ${from}`);
      if (states.load.isReloadScheduled) {
        return;
      }
      const currentUrl = `${location.origin}${location.pathname}`;
      const isChangedUrl = currentUrl !== states.load.lastUrl;
      if (!isChangedUrl) {
        return;
      }
      colorLog.detail("URL has changed.");
      scheduleReload();
    };
    window.addEventListener("popstate", () => {
      checkForUrlChange("popstate");
    });
    window.addEventListener("hashchange", () => {
      checkForUrlChange("hashchange");
    });
    document.addEventListener("turbo:load", () => {
      checkForUrlChange("turbo:load");
    });
    document.addEventListener("turbo:render", () => {
      checkForUrlChange("turbo:render");
    });
    history.pushState = function(...args) {
      originalPushState.apply(this, args);
      checkForUrlChange("pushState");
    };
    history.replaceState = function(...args) {
      originalReplaceState.apply(this, args);
      checkForUrlChange("replaceState");
    };
  }
  function watchHotkeys() {
    if (document.documentElement.dataset.hotkeysBound) {
      return;
    }
    document.documentElement.dataset.hotkeysBound = "true";
    document.addEventListener("keydown", (event2) => {
      const keyAlt = event2.altKey;
      const keyCmd = event2.metaKey;
      const keyCtrl = event2.ctrlKey;
      const keyShift = event2.shiftKey;
      const isCmdShift = keyCmd && keyShift;
      const isCmdCtrl = keyCmd && keyCtrl;
      let modifier;
      if (isCmdShift) modifier = "cmdShift";
      if (isCmdCtrl) modifier = "cmdCtrl";
      if (event2.repeat || !isCmdShift && !isCmdCtrl || isCmdShift && keyCtrl || isCmdCtrl && keyShift || keyAlt)
        return;
      if (isCmdShift) {
        if (event2.code !== "Digit1" && event2.code !== "Digit2" && event2.code !== "KeyE") return;
      } else if (isCmdCtrl) {
        if (event2.code !== "Digit1" && event2.code !== "Digit2" && event2.code !== "Digit3" && event2.code !== "Digit4" && event2.code !== "Digit5" && event2.code !== "KeyB" && event2.code !== "KeyC" && event2.code !== "KeyE" && event2.code !== "KeyM" && event2.code !== "KeyN" && event2.code !== "KeyR" && event2.code !== "KeyT" && event2.code !== "Comma")
          return;
      }
      activateHotkey(modifier, event2.code);
    });
  }
  function watchPromptSubmission() {
    const lsbotPromptInputs = document.querySelectorAll(".lsbot-question-input");
    if (lsbotPromptInputs.length < 1) return;
    lsbotPromptInputs.forEach((prompt) => {
      if (prompt.dataset.focusObserverBound) {
        return;
      }
      prompt.dataset.focusObserverBound = "true";
      let observer = null;
      prompt.addEventListener("focus", () => {
        observer?.disconnect();
        observer = new MutationObserver(() => {
          if (!prompt.disabled) {
            observer.disconnect();
            prompt.focus();
          }
        });
        observer.observe(prompt, {
          attributes: true,
          attributeFilter: ["disabled"]
        });
      });
    });
  }
  function watchQuestionBoxes() {
    const questionBoxes = document.querySelectorAll(".lsbot-question-box");
    if (questionBoxes.length < 1 || !elements.native.tabsPanel) return;
    const lsbotTabBtn = document.querySelector(".tab-button[data-tab='lsbot-help']");
    const handleSubmitClick = () => {
      handleFocus(lsbotTabBtn);
    };
    const handleSubmitHotkey = () => {
      const isCmdEnter = event.metaKey && !event.ctrlKey && !event.altKey && !event.shiftKey && event.key === "Enter";
      if (!isCmdEnter) return;
      handleFocus(lsbotTabBtn);
    };
    questionBoxes.forEach((box) => {
      if (box.dataset.questionEventBound) {
        return;
      }
      box.dataset.questionEventBound = "true";
      const boxSendLink = box.querySelector(".lsbot-question-link");
      const boxSubmitButton = box.querySelector(".lsbot-question-box-send-answer-button");
      boxSendLink.addEventListener("click", handleSubmitClick);
      boxSubmitButton.addEventListener("click", handleSubmitClick);
      const boxTextarea = box.querySelector(".lsbot-question-box-answer-input");
      boxTextarea.addEventListener("focus", () => boxTextarea.addEventListener("keydown", handleSubmitHotkey));
      boxTextarea.addEventListener("blur", () => boxTextarea.removeEventListener("keydown", handleSubmitHotkey));
    });
  }
  function watchSettingMenuToggleBtn() {
    const settingsMenuToggleBtn = elements.injected.settingsToggleButton;
    if (!settingsMenuToggleBtn) return;
    if (settingsMenuToggleBtn.dataset.settingsMenuToggleBtnEventBound) {
      return;
    }
    settingsMenuToggleBtn.dataset.settingsMenuToggleBtnEventBound = "true";
    settingsMenuToggleBtn.addEventListener("click", () => toggleSettingsMenu());
  }
  function watchShowSidebarBtn() {
    const sidebarShowBtn = elements.injected.sidebarShowButton;
    if (!sidebarShowBtn) return;
    if (sidebarShowBtn.dataset.sidebarBtnEventBound) {
      return;
    }
    sidebarShowBtn.dataset.sidebarBtnEventBound = "true";
    sidebarShowBtn.addEventListener("click", () => showSidebar());
  }
  function watchTabBtnClick() {
    const tabButtons = document.querySelectorAll(".tab-button");
    if (tabButtons.length < 1) return;
    tabButtons.forEach((tabBtn) => {
      if (tabBtn.dataset.tabBtnEventBound) {
        return;
      }
      tabBtn.dataset.tabBtnEventBound = "true";
      tabBtn.addEventListener("click", () => {
        handleFocus(tabBtn);
      });
    });
  }
  function watchTabsPanelToggleBtn() {
    const tabsPanelToggleBtn = elements.injected.tabsPanelToggleButton;
    if (!tabsPanelToggleBtn) return;
    if (tabsPanelToggleBtn.dataset.tabsPanelToggleBtnEventBound) {
      return;
    }
    tabsPanelToggleBtn.dataset.tabsPanelToggleBtnEventBound = "true";
    tabsPanelToggleBtn.addEventListener("click", () => toggleTabsPanel());
  }

  // src/js/modules/hotkeys-menu.js
  function injectHotkeysMenu() {
    const settingsMenu = document.querySelector(".settings-menu");
    if (!settingsMenu) return;
    const createHotkeysMenu = () => {
      const hotkeysMenuEl = document.createElement("section");
      hotkeysMenuEl.classList.add("hotkeys-section");
      const hotkeysMenuTitleEl = document.createElement("h3");
      hotkeysMenuTitleEl.classList.add("section-title");
      hotkeysMenuTitleEl.innerText = "Current Page Hotkeys";
      hotkeysMenuEl.append(hotkeysMenuTitleEl);
      const modifiersWrapperEl = document.createElement("div");
      modifiersWrapperEl.classList.add("settings-list-wrapper");
      for (const [modifierListKey, modifierListObj] of Object.entries(states.hotkeys)) {
        let modifierKey;
        if (modifierListKey === "cmdShift") modifierKey = "Shift";
        if (modifierListKey === "cmdCtrl") modifierKey = "Ctrl";
        const modifierListEl = document.createElement("ul");
        modifierListEl.classList.add("settings-list");
        const modifierListTitleEl = document.createElement("li");
        modifierListTitleEl.classList.add("settings-list__title");
        modifierListTitleEl.innerText = `${modifierKey} Shortcuts`;
        modifierListEl.append(modifierListTitleEl);
        for (const hotkeyObj of Object.values(modifierListObj)) {
          const hotkeyItemEl = document.createElement("li");
          hotkeyItemEl.classList.add("settings-list__item");
          const hotkeyItemKeyEl = document.createElement("div");
          hotkeyItemKeyEl.classList.add("setting-status", "hotkey-shortcut");
          const keys = ["Cmd", modifierKey, hotkeyObj.symbol];
          keys.forEach((key, index) => {
            const keySpan = document.createElement("span");
            keySpan.classList.add("key");
            keySpan.innerText = key;
            hotkeyItemKeyEl.append(keySpan);
            const isLast = index === keys.length - 1;
            if (isLast) return;
            const plusSpan = document.createElement("span");
            plusSpan.innerText = "+";
            hotkeyItemKeyEl.append(plusSpan);
          });
          hotkeyItemEl.append(hotkeyItemKeyEl);
          const hotkeyItemLabelEl = document.createElement("div");
          hotkeyItemLabelEl.classList.add("setting-desc", "hotkey-label");
          hotkeyItemLabelEl.innerText = hotkeyObj.label;
          hotkeyItemEl.append(hotkeyItemLabelEl);
          modifierListEl.append(hotkeyItemEl);
        }
        modifiersWrapperEl.append(modifierListEl);
      }
      hotkeysMenuEl.append(modifiersWrapperEl);
      return hotkeysMenuEl;
    };
    settingsMenu.append(createHotkeysMenu());
  }

  // src/js/modules/helpers/load.js
  function loadUI() {
    injectStyles();
    syncNativeElementsState();
    injectHeader();
    injectToaster();
    syncInjectedElementsState();
    syncAvailableHotkeys();
    injectHotkeysMenu();
    if (elements.native.tabsPanel) {
      if (states.hidden.isTabsPanelHidden) {
        hideTabsPanel();
      } else {
        showTabsPanel();
      }
    }
    if (elements.injected.header && states.hidden.isHeaderHidden) hideHeader();
    watchForUrlChange();
    watchForMissingHeader();
    watchHotkeys();
    watchPromptSubmission();
    watchQuestionBoxes();
    watchShowSidebarBtn();
    watchTabBtnClick();
    watchTabsPanelToggleBtn();
    watchSettingMenuToggleBtn();
  }
  function scheduleReload() {
    if (states.load.isReloadScheduled) {
      return;
    }
    setIsReloadScheduled(true);
    const reloadUI = () => {
      loadUI();
      setLastUrl();
      setIsReloadScheduled(false);
    };
    const startWait = performance.now();
    const waitForDom = () => {
      const isNewBody = document.body !== states.load.previousBody;
      const isWaitMaxReached = performance.now() - startWait > 3e3;
      if (isNewBody) {
        requestAnimationFrame(reloadUI);
        return;
      } else if (isWaitMaxReached) {
        requestAnimationFrame(reloadUI);
        return;
      } else {
        requestAnimationFrame(waitForDom);
      }
    };
    requestAnimationFrame(waitForDom);
  }

  // src/js/index.js
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", init, { once: true });
  } else {
    init();
  }
  function init() {
    setLastUrl(`${location.origin}${location.pathname}`);
    setPreviousBody(document.body);
    loadUI();
  }
})();
